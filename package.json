{
  "name": "asc-aria-monorepo",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "workspaces": [
    "web"
  ],
  "scripts": {
    "dev": "npm --prefix web run dev --",
    "build": "npm --prefix web run build",
    "lint": "npm --prefix web run lint",
    "preview": "npm --prefix web run preview --"
  }
}
